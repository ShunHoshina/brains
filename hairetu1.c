#include <stdio.h>

int main (void){
	int a, b = 0,c, d;
  int data[10];
  for(a = 0; a < 10; a++){	
  scanf("%d",&data[a]);
  }
  
	
	for(d = 0; d < 10 ;d++){
	b +=data[d];
	}
  printf("%d\n", b);
	return (0);
}
	
